import React from "react";

const useFetchUuid = () => {
  // const [result, setResult] = useState<Root>();
  // const [error, setError] = useState();
  // const [isLoading, setIsLoading] = useState(true);

  return <div>useFetchUuid</div>;
};

export default useFetchUuid;
